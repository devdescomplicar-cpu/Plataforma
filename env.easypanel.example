# =============================================================================
# Variáveis para o Easypanel – UM SÓ LUGAR
# Cole estas no painel do app (Environment) e preencha os valores.
# O server usa todas em runtime; o front é estático (mesma porta).
#
# Build Arguments (opcional): se o Easypanel tiver "Build Args", defina
# VITE_APP_URL e VITE_API_URL iguais aos de baixo para o front já ser
# construído com a URL correta. Senão, deixe em branco: a API fica em /api.
# =============================================================================

# --- Obrigatórias ---
DATABASE_URL=postgresql://usuario:senha@host:5432/nome_do_banco
JWT_SECRET=uma-chave-secreta-forte-mude-em-producao
VITE_APP_URL=https://seudominio.com

# --- Build (opcional): se o Easypanel tiver "Build Arguments", use os mesmos valores ---
# Assim o front (links, API) já sai com a URL certa na imagem.
# VITE_APP_URL=https://seudominio.com
# VITE_API_URL=
# (VITE_API_URL vazio = API em /api, mesma origem – o normal no Docker)

# --- Servidor ---
PORT=3000
NODE_ENV=production

# --- MinIO (storage de imagens). Bucket é criado automaticamente se não existir ---
MINIO_ENDPOINT=minio
MINIO_PORT=9000
MINIO_ACCESS_KEY=minioadmin
MINIO_SECRET_KEY=minioadmin
MINIO_BUCKET=vehicle-images
MINIO_USE_SSL=false

# --- JWT ---
JWT_EXPIRES_IN=7d

# --- CORS (se o front for acessado de outro domínio; mesma origem pode deixar vazio) ---
# CORS_ORIGINS=https://seudominio.com,https://www.seudominio.com

# --- PWA / Push (opcional). Gerar: npx web-push generate-vapid-keys ---
# VAPID_PUBLIC_KEY=
# VAPID_PRIVATE_KEY=
# VAPID_MAILTO=mailto:support@seudominio.com

# --- Admin / Storage (opcional) ---
# STORAGE_TOTAL_MB=61440
# STORAGE_RETENTION_POLICY=true

# --- Webhook reprocess (opcional) ---
# WEBHOOK_REPROCESS_SECRET=reprocess-internal

# --- Criptografia (opcional; fallback usa JWT_SECRET) ---
# ENCRYPTION_KEY=
